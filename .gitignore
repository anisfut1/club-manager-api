node_modules/
dist/
.env
.env.local
.vercel/
*.log

# tesseract.js écrit un cache de langue à la racine du process (cwd) par
# défaut (aucun `cachePath` configuré, voir pdf-raster-ocr-extractor.ts) —
# jamais le vrai fichier vendorisé (src/integrations/emarque/ocr-data/).
/fra.traineddata
